import PropTypes from "prop-types";

const PostCardContent = ({ images }) => {
  return <div>구현중</div>;
};

PostCardContent.propTypes = {
  images: PropTypes.arrayOf(PropTypes.object).isRequired,
};

export default PostCardContent;
